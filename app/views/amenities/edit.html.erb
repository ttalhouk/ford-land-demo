<h1>Editing Amenity</h1>
<%= form_for (@amenity), html: { multipart: true } do |f| %>
  <%= render 'form', {amenity: @amenity, f: f, pictures: @pictures} %>
<% end %>
<%= link_to 'Show', @amenity %> |
<%= link_to 'Back', amenities_path %>

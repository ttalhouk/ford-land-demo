<h2>Editing Service Request</h2>

<%= form_for [:admin, @service], html: { multipart: true } do |f| %>
  <%= render 'form', service: @service, f: f %>
<% end %>
<%= link_to 'Back', admin_services_path, class:'btn btn-primary btn-sm' %>

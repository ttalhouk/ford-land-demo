<h1>Editing Property</h1>

<%= form_for :property , html: { multipart: true } do |f| %>
  <%= render 'form', property: @property, document: @document, f: f, pictures: @pictures %>
<% end %>
<%= link_to 'Show', @property %> |
<%= link_to 'Back', properties_path %>
